---
import Layout from "@/layouts/Layout.astro";
import LoginForm from "@/components/auth/LoginForm";

// Check if user is already authenticated
// Note: Middleware will handle this redirect automatically,
// but we keep this check for explicit clarity and to avoid unnecessary rendering
const token = Astro.cookies.get("sb-access-token");

if (token?.value) {
  return Astro.redirect("/profiles");
}
---

<Layout title="Logowanie - Rytmik">
  <div class="min-h-screen flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
    <div class="w-full max-w-md space-y-8">
      {/* Header */}
      <div class="text-center">
        <a href="/" class="inline-block mb-6">
          <h1 class="text-4xl font-bold text-primary">Rytmik</h1>
        </a>
        <h2 class="text-2xl font-bold">Zaloguj się</h2>
        <p class="mt-2 text-sm text-muted-foreground">Witaj ponownie! Zaloguj się, aby kontynuować</p>
      </div>

      {/* Login Form */}
      <div class="bg-card border rounded-lg shadow-sm p-6 sm:p-8">
        <LoginForm client:load />
      </div>

      {/* Demo link */}
      <div class="text-center">
        <p class="text-sm text-muted-foreground">
          Chcesz najpierw wypróbować?{" "}
          <a href="/demo" class="font-medium text-primary hover:underline"> Przejdź do demo </a>
        </p>
      </div>
    </div>
  </div>
</Layout>
